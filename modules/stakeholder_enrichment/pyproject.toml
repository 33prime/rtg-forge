[project]
name = "stakeholder-enrichment"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "rtg-core",
    "httpx>=0.27",
    "langchain-anthropic>=0.3",
    "langgraph>=0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
