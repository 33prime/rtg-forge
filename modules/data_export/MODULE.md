# Export any entity as CSV, JSON, or PDF with filtering, background generation for large sets, and download link delivery

Stub — implementation will be extracted from a real project via `/add-module`.
