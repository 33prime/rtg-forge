# Runtime feature flags with per-user, per-org, and percentage-based rollout — DB-backed, no external service dependency

Stub — implementation will be extracted from a real project via `/add-module`.
